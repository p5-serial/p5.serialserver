// this is an example code to upload to your Arduino
// it will open the serial port at the specified baudRate
// and then write a byte on the serial port
// the byte will alternate between the values 1 and 0
// between each send it waits the specified time

// int baudRate = 9600;
// int waitTime = 1000;

//byte info;

//void setup() {
  // Serial.begin(baudRate);
//}

//void loop() {
  
//  if (info == 0) {
//    info = 1;
//  }
//  else {
//    info = 0;
//  }
  
//  Serial.write(info);
//  delay(waitTime);
//}
